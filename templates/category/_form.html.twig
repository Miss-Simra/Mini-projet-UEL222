{{ form_start(form) }}

<div class="form-group row">

    <div class="col-sm-1 col-form-label">
    {{ form_label(form.name)  }}
     </div>

    <div class="col-sm-1">
    {{ form_widget(form.name, { 'attr': {'class': 'form-control'} }) }}


     </div>

    <div class="d-flex">

    <button class="m-4 btn btn-secondary">{{ button_label|default('Save') }}</button>

    </div>

{{ form_end(form) }}
